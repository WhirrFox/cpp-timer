
#ifndef TIMER_SETTINGS_H
#define TIMER_SETTINGS_H

// Location of log file (default: "log.csv")
#define filename "log.csv"

// History: Number of entries per page (default: 3)
#define perPage 3

// Timer: Divider of break time (default: 5)
#define timerDivider 5

// Timer: Turn on sound effect on timer completion (Windows only, default: true)
#define enableSound true

#endif //TIMER_SETTINGS_H
